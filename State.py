# import copy


# class State:
#      ## the state will hold only the changing object, packages dissappears, delivers not dissapper
#     def __init__(self,environemnt,cur_location) -> None:
#         self.env = environemnt
#         self.current_location = cur_location
#         self.cur_huristic_val = 0
#         self.prev_state = None
#         self.packages, self.deliveries = {},{}
#         for package in environemnt.packages:
#             self.packages[package.id] = package.cur_location
#             self.deliveries[package.id] = package.dst_location  ## it doesnt matter which one is first?
            
            
            
#     def update_prev_state(self, prev_state):
#         self.prev_state = copy.copy(prev_state)
            
            


        